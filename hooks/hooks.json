{
  "description": "Starts the ContextSpin daemon at session start so the spinner/statusline has fresh org context.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd /tmp && npx --yes contextspin@0.7.0 ensure >/dev/null 2>&1; exit 0",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
